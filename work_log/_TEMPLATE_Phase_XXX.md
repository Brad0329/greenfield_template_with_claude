# Phase XXX: <이름> (완료일: YYYY-MM-DD)

> 코드에서 알 수 없는 것만 쓴다. 코드 구현 상세·git 히스토리·테스트 케이스 목록은 쓰지 않는다.

## 결정의 이유
- <"A와 B 중 A 선택 — 이유: ..."> (코드는 What, 로그는 Why)
- **버린 대안과 그 이유도 함께.** 이게 없으면 다음 세션이 같은 대안을 다시 검토한다.

## 외부 제약
- <발급 절차, 인코딩·대소문자 규칙, 플랫폼 제약, 라이선스 의무 등 코드에 안 드러나는 것>

## 실패한 접근과 원인 ★가장 값이 큰 항목
- <"X 시도 → 실패 → 원인 Y → Z로 해결"> — 다음 세션이 같은 실수를 반복하지 않도록
- 원인을 엉뚱한 데서 찾았다면 **그 오해도 적는다** (같은 함정에 또 빠진다)

## 향후 운영상 주의점
- <재생성 불가능한 자산, 수동 절차, 만료되는 것, 갱신 시 함께 고쳐야 하는 것>

## 검증 결과
- 테스트: <실행한 것과 결과. 수용 기준 중 무엇을 확인했는지 범위 명시>
- 수용 기준 대조: <✅ N개 / 이탈 처리 내역>
- **대체 수단을 썼다면 그 사실과 이유**: <예: 에이전트 호출 불가 → 전체 테스트로 대체 검증>
- 사용자 실테스트 대기: <항목 → plan.md에도 반영>
- REQUIREMENTS.md 갱신: <이번 Phase에서 바뀐 요구사항이 반영됐는지>
