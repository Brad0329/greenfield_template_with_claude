# Phase XXX: <이름> (완료일: YYYY-MM-DD)

> **실패한 접근·버린 대안이 있을 때만 쓴다.** 없으면 이 파일을 만들지 말고 plan.md 체크박스 옆에
> 완료일만 적는다. 코드 구현 상세·git 히스토리·테스트 목록·검증 결과는 쓰지 않는다
> (검증 결과는 커밋 메시지에). 코드는 What, 로그는 Why.

## 실패한 접근과 원인 ★
- <"X 시도 → 실패 → 원인 Y → Z로 해결"> — 원인을 엉뚱한 데서 찾았다면 **그 오해도 적는다**
  (같은 함정에 또 빠진다).

## 다음 세션이 알아야 할 것 (있는 것만)
- 결정과 버린 대안: <"A와 B 중 A — 이유: … / B는 …라서 기각"> — 없으면 다음 세션이 B를 다시 검토한다
- 외부 제약: <발급 절차, 인코딩·대소문자 규칙, 플랫폼 제약, 라이선스 의무>
- 운영 주의점: <재생성 불가능한 자산, 수동 절차, 만료되는 것, 갱신 시 함께 고쳐야 하는 것>
- 사용자 실테스트 대기: <항목 → plan.md에도 반영>
