# 신규(Greenfield) 시스템 개발 — AI 코딩 스타터 템플릿

완전히 새로운 시스템을 처음부터 만드는 프로젝트를 Claude Code로 진행할 때
**첫날부터 장착하고 시작하는** 템플릿.

레거시 재구축 템플릿(`legacy-template-with-ai-coding`)의 자매판 — 원본 대조/데이터 이관 장치를
걷어내고, 그 자리에 **요구사항 명세를 기준점**으로 하는 장치를 넣었다.

**스택 중립적이다.** 웹·앱·CLI·라이브러리 어디에도 쓸 수 있게 특정 스택의 명령·경로를 넣지 않았다.
빈칸은 초기화 때 채운다.

## 사용법

1. 새 프로젝트 루트에 이 폴더 내용을 복사한다(`.claude/`는 숨김 폴더라 빠지기 쉽다).
2. Claude Code에서 그 폴더를 열고 **이렇게 말한다**:

   ```
   새 프로젝트 시작하자. 초기화부터 하자.
   <프로젝트 이름> — <한 줄 설명>. 스택은 <스택>으로 갈 생각이다.
   ```

   `CLAUDE.md`는 자동으로 읽히고, `<...>` 플레이스홀더가 남아 있으면 **초기화 체크리스트를 먼저
   진행**하게 되어 있다. **이 README를 읽으라고 시킬 필요는 없다** — 사람이 읽는 문서이고,
   초기화 마지막 단계에서 지운다.
3. 못 채우는 칸은 지우지 말고 "미정"으로 남긴다 — 나중에 그 결정을 해야 한다는 표시가 된다.

```
CLAUDE.md                        ← 규칙·정책. 초기화 체크리스트 내장
docs/REQUIREMENTS.md             ← 요구사항 단일 원본 (제일 먼저 채울 것)
docs/SCHEMA.md                   ← 스키마 변경 결정 기록 + 승인 게이트
docs/playbooks/_TEMPLATE.md      ← 사고 1건 = 파일 1개 (새 사고는 이 형식으로 추가)
docs/playbooks/노하우_*.md       ← 스택 중립 노하우 6종 — 항상 유지
docs/playbooks/팩_*.md           ← 선택형 스택 팩 5종 — 해당 없으면 초기화 때 삭제
work_log/plan.md                 ← 전체 계획 단일 원본
work_log/_TEMPLATE_Phase_XXX.md  ← Phase 완료 로그
.claude/agents/*.md              ← 선택적 에이전트 4종 (검증 3 + 조사 1)
.claude/skills/approval-audit/   ← /approval-audit — Phase 종료 시 승인 대기 원인 점검 (파이썬 3.10+)
.claude/skills/debt-audit/       ← /debt-audit — Phase 3~5개마다 부채 점검(반복 실수·중복 로직·복수 원본). 목록만, 결정은 사용자
.claude/hooks/*.py               ← 습관 차단 훅 4종: cd 접두사·출력 필터·flutter test 인자·인라인 파이썬 (settings.json에 등록)
scripts/measure_approvals.py     ← 승인 대기 원인 분류(예측) · measure_wait.py ← 벽시계 실측 (테스트 동봉)
.claude/settings.json            ← deny(안전장치) + 훅 등록. allow는 비어 있다 — 규칙은 settings.local.json에(효력이 거기만 있다)
```

## 동봉된 노하우·팩 (실전 프로젝트 1회분의 교훈 환류)

앞선 실전 프로젝트(hanjadic, 2026-08)의 작업 로그 전체에서 추출한 교훈이 `docs/playbooks/`에
들어 있다. 수치가 붙은 항목은 전부 그 프로젝트의 실측값이다.

| 파일 | 내용 | 처분 |
|---|---|---|
| `노하우_승인_대기_최소화` | Claude Code 허용 규칙 공학 — 한 세션 실측에서 승인 대기가 테스트의 12배였다 | 유지 |
| `노하우_검증` | 테스트가 실제로 잡게 만드는 법(변이 확인·검증의 층·디버깅) | 유지 |
| `노하우_설계` | 데이터 수명·실패 처리·보안 기본값·구조 | 유지 |
| `노하우_외부API_데이터` | 문서 대신 실측 — 200이 성공이 아닐 수 있다 | 유지 |
| `노하우_프로세스` | 기록 시점·상태 어휘·커밋 단위·"문서는 두 번째 방어선" | 유지 |
| `노하우_작업_트랙` | 저위험/일반 트랙 — 기준은 "되돌리기 비용". 판이 세 번 바뀐 역사 | 유지 |
| `팩_Flutter_Android` | 모바일 앱 빌드·위젯 테스트·플랫폼 함정 | 해당 없으면 삭제 |
| `팩_Windows_PowerShell` | 인코딩·PowerShell·Windows 함정 | 해당 없으면 삭제 |
| `팩_웹_PWA` | 서비스워커·보안 컨텍스트·MIME | 해당 없으면 삭제 |
| `팩_MCP` | MCP 서버 프로토콜·클라이언트별 등록 | 해당 없으면 삭제 |
| `팩_LLM_생성기능` | LLM API 비용·형식·프롬프트 개방 | 해당 없으면 삭제 |

## ★ 초기화 때 반드시 하는 것

**허용 규칙은 `.claude/settings.local.json`에 만든다 — `settings.json`의 `allow`는 비어 있고,
채워도 효력이 없다** (2026-08-22 실측: 세션 전부터 있던 규칙이 물었다. 같은 파일의 `hooks`·
`env`·`deny`는 그대로 둔다). local은 gitignore라 공유되는 허용 규칙은 불가능하다 — 초기화 때
`노하우_승인_대기_최소화.md`의 **settings.local.json 스타터**를 붙여 넣고 이 스택에 없는 줄을
뺀 뒤, 자주 쓰는 호출은 "다시 묻지 않기"로 쌓아 간다. 와일드카드는 작동하지만 **패턴에
역슬래시가 있으면 죽는다** — 경로는 슬래시로. **틀린 기본값은 빈 칸보다 나쁘다** — 죽은 파일에
규칙을 두면 "규칙이 있는데 왜 묻지"라는 오진을 만든다(같은 자리에서 세 번 틀린 실사례).

- 규칙 문법: `"Bash(git log *)"` = 프리픽스 매칭, `"Bash(npm test)"` = 정확 일치. `deny`가 우선.
  ⚠️ **규칙은 `settings.local.json`에 있어야 효력이 있고, 패턴에 역슬래시가 있으면 죽는다**
  (2026-08-22 실측 — 종전 "와일드카드 무효" 결론은 죽은 파일 위의 실험이라 취소됐다).
  와일드카드는 인자가 매번 달라지는 자리에만 좁게 열고, 나머지는 **호출 문자열을 고정**한다.
- **`Bash(...)`와 `PowerShell(...)` 규칙은 별개다.** 이 환경의 기본 셸 도구가 무엇인지 먼저 확인.
- **규칙을 추측하지 말 것** — 사용자가 확인 창에서 "다시 묻지 않기"를 한 번 고르면 매칭 코드가
  `settings.local.json`에 정답 패턴을 적는다. 그걸 `settings.json`으로 옮기는 게 확실하다.
  규칙 검증은 자동 허용 부류(`git status`·`git log` 등)가 아니라 **실제로 묻던 명령**으로 한다.
- **`cd X && ...` 복합 명령이나 `FOO=bar cmd` 접두사는 규칙에 안 걸린다.**
  환경변수는 `settings.json`의 `env`에 두고, `cd`는 애초에 쓰지 않는다(작업 디렉토리는 유지된다).
- 상세: `docs/playbooks/노하우_승인_대기_최소화.md`
- `.claude/settings.json` = git 커밋(공통) · `.claude/settings.local.json` = gitignore(개인).
- `defaultMode: "acceptEdits"` 포함 — 혼자 작업 기준. **팀이면 이 줄을 개인 설정으로 옮길 것.**
- 며칠 써본 뒤 `/fewer-permission-prompts`로 실사용 기반 갱신.

## 지식 3분할 원칙 (어디에 뭘 기록하나)

| 위치 | 내용 |
|---|---|
| **CLAUDE.md** | 매 작업마다 지켜야 할 규칙·정책·함정 목록. 짧게, 링크 위주. **상한 규칙 100개**(`scripts/count_claude_rules.py`) — 넘으면 이관(본문 'CLAUDE.md 비대화 방지') |
| **docs/REQUIREMENTS.md** | 기능 요구사항 + 수용 기준의 단일 원본 (+ ID 대장은 CLAUDE.md) |
| **docs/SCHEMA.md** | 스키마 변경 **결정**과 이유, 승인 게이트 (현재 상태는 코드가 정답) |
| **work_log/plan.md** | 전체 계획·Phase 체크리스트·**보류 항목**의 단일 원본 |
| **work_log/Phase_XXX.md** | "코드에서 알 수 없는 것만" — 결정 이유·외부 제약·**실패한 접근**·운영 주의점 |
| **docs/playbooks/*.md** | 증상→원인→해결 상세. 사고 한 번 = 파일 하나 |
| **개인 메모리** | 접속정보, 개인 노하우 — **git에 안 들어가는 것** |

- **비밀정보(키·토큰·비밀번호)는 git·CLAUDE.md·채팅에 절대 넣지 않는다.** `.env` + gitignore.
- 사고를 겪으면: 플레이북 작성 → CLAUDE.md 함정 목록에 한 줄 + 링크.
- **요구사항이 바뀌면 그 자리에서 REQUIREMENTS.md를 고친다** — 대화 속 합의로만 남기면
  다음 세션이 구버전으로 구현한다.

## 에이전트 4종 — 있으면 쓰고, 없으면 대체한다

| 에이전트 | 시점 | 역할 |
|---|---|---|
| `researcher` | 조사 단계(Phase 001 등) | 데이터 소스·외부 API·라이선스를 **실측 근거와 함께** 조사 보고. 독립 조사는 병렬 인스턴스로 — 메인은 결론만 받는다 |
| `requirements-analyst` | 구현 **전** | 요구사항을 구현 가능한 스펙으로 + **모호점을 질문 목록으로** |
| `spec-checker` | 완료 **후** | 구현 vs 수용 기준 대조, 누락·조용한 축소·임의 추가 검출 |
| `qa-tester` | Phase 완료 | 통합 테스트 게이트(foreground). 코드를 고치지 않고 판정만 |

**환경에 따라 서브에이전트 호출이 안 될 수 있다.** 그때는 CLAUDE.md '검증 게이트' 표의
대체 수단으로 가되, **검증 자체를 건너뛰지 않고 그 사실을 작업 로그에 남긴다.**

## Phase 사이클

```
plan.md에서 Phase 확인 → 수용 기준 확정(REQUIREMENTS.md) → 구현
→ 전체 테스트 통과 → 수용 기준 대조 → (가능하면 /code-review)
→ plan.md 체크 [x] + Phase_XXX.md 작성 → 다음 Phase
```

**가장 위험한 것을 먼저 한다.** 쉬운 것부터 하면 나중에 못 하는 걸 발견한다.

## 기본 탑재된 함정 목록

1. **요구사항 없이 구현 시작** — "만들다 보면 알겠지"는 스코프 크리프의 시작
2. **과잉 설계(YAGNI)** — 아직 없는 요구사항을 위한 추상화 금지
3. **Silent failure** — 조용히 삼킨 예외는 "0건 처리"를 "정상 완료"로 둔갑시킨다
4. **조용한 절단** — 결과를 자르면 잘랐다는 사실과 전체 건수를 함께 준다
5. **무단 스키마 변경** — 되돌리기 비싸다. SCHEMA.md 기록 + 사용자 확인 후
6. **템플릿 기본값을 그대로 쓰기** — 이 프로젝트 것이 아니다. 초기화 때 갈아엎는다
7. **손으로 만든 데이터** — 사람이 쓴 매핑·목록은 반드시 틀린다. 자동 검증을 붙인다
8. **통과했다고 검증된 게 아니다** — 새 테스트는 일부러 틀리게 해서 실패를 한 번 본다
9. **"됐다"를 빌드 성공으로 판단** — 화면·응답·데이터로 확인한다
10. **환경별 설정 통째 복사** — 값이 다르다. 필요한 키만 한 줄씩
11. **표본 2건으로 결론 내리기** — 실측이 아니다. 표본 수를 기록하고, 널뛰는 값은 분포를 잰다
12. **느리다고 테스트부터 줄이기** — 병목을 재면 승인 대기인 경우가 있다(실측 12배 차이)
13. **로그에 적으면 안 밟을 거라는 착각** — 문서는 두 번째 방어선. 컴파일러·테스트로 강제한다
14. **조용한 무반응을 코드에서 찾기** — 오류 없는 무반응은 권한·매니페스트·등록 누락부터 본다
