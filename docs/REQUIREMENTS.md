# <프로젝트명> 요구사항 (단일 원본)

> 기능 요구사항 + 수용 기준의 단일 원본. **요구사항이 바뀌면 대화로 합의만 하지 말고 이 파일을 즉시 갱신.**
> 완료 판정은 항상 이 문서를 기준으로 한다(에이전트를 쓰든 직접 대조하든).
> **새 F-번호는 CLAUDE.md의 '요구사항 ID 대장'에서 받아간다. 번호 재사용 금지.**

## 시스템 목표
- <이 시스템이 해결하는 문제 한 단락. 누가(사용자군), 무엇을, 왜>

## 사용자/권한
- <역할 목록과 각 역할이 할 수 있는 것 — 예: 비회원(조회만) / 회원 / 관리자>

## 화면 방향 (UI가 있는 프로젝트만 — 없으면 이 절을 지운다)

> **게이트: 화면을 그리는 첫 기능을 구현하기 전에 이 절이 채워져 있어야 한다.** 비어 있으면 그 자리에서
> 사용자에게 묻는다(CLAUDE.md '요구사항 정책'). 초기화 때 채우지 않는다 — 제품이 뭔지 알아야 답할 수 있다.
> 사용자는 이 결정을 잊는 것이 정상이고, 기억하는 쪽은 AI다.

1. **UI 형태**: <웹 / 모바일 앱 / 데스크톱 / CLI(→ 이 절 삭제)>
2. **기준 기기·해상도**: <예: 모바일 우선(360px), 데스크톱은 확장 / 데스크톱 전용 1280px>
3. **룩앤필의 기준 — 셋 중 하나를 고른다**:
   <(a) 참고 앱·사이트 2~3개(이름·어떤 점을 닮을지) / (b) 디자인 시스템(Material·Cupertino·shadcn 등) /
   (c) 프레임워크 기본 컴포넌트 그대로 — 고르지 않으면 AI의 기본값이 곧 디자인이 된다>
4. **다크모드 / 접근성 / 언어**: <예: 다크모드 없음, 글자 크기 시스템 설정 따름, 한국어만>
5. **첫 화면 확정 방식**: 말로 합의하지 않는다 — <렌더링·HTML 한 장·목업 중 이 환경에서 되는 수단>으로
   그려서 보여준 뒤 확정하고, 확정본을 여기 링크한다(`노하우_프로세스.md` 화면/UI 결정).

## 기능 요구사항

### F-001: <기능명>
- **설명**: <한 문단>
- **수용 기준** (검증 가능한 "~하면 ~된다" 문장으로):
  - [ ] <예: 필수 항목 누락 시 저장되지 않고 해당 필드에 오류가 표시된다>
  - [ ] <예: 저장 성공 시 목록 화면으로 이동하고 새 항목이 최상단에 보인다>
- **엣지케이스**: <빈 값 / 권한 없음 / 경계값 각각의 기대 동작>
- **상태**: 미착수 | 진행중(Phase XXX) | 완료

### F-002: <작은 기능은 이 한 줄 형태> — <한 줄 설명>
- **수용 기준**: [ ] <한두 개> · **상태**: 미착수
- (설명·엣지케이스 절은 필요해질 때 F-001 형태로 늘린다. 형태와 무관하게 번호는 대장에서 받는다.)

## 비기능 요구사항
- <성능/보안/브라우저 지원/접근성 등 — 예: 목록 응답 1초 이내, 모바일 뷰 지원>

## 하지 않는 것 (Out of Scope)
- <명시적으로 범위 밖인 것 — YAGNI 방어선. 예: 다국어, 앱, 외부 API 공개>

## 미결 질문
- <아직 답을 못 받은 모호점 — 답이 나오면 해당 기능 섹션에 반영하고 여기서 삭제>
