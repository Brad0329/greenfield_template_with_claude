# 노하우: 승인 대기 최소화 (Claude Code 허용 규칙 공학)

> **독자: AI 에이전트.** 사용자는 읽지 않아도 된다. `/approval-audit`이 규칙·훅을 손댈 때,
> 승인 창이 반복될 때, 훅을 고칠 때 1부를 본다. 2부는 "왜 그 결론인가"를 확인할 때만 연다.
>
> **구조**: 1부 = 현재 결론(규칙만). 2부 = 실측 이력(날짜순, 뒤집힌 결론도 남긴다 — 근거 데이터가
> 바뀌면 결론을 다시 볼 수 있어야 한다). 1부의 `[H#]`는 2부의 해당 항목이다.
> **새 실측은 2부에 날짜로 추가하고, 결론이 바뀌면 1부를 고친다.** 1부에 이력을 쌓지 않는다.

---

# 1부 — 현재 결론

## 1. 원칙: 허용의 기준은 "위험" (2026-08-22 사용자 결정, CLAUDE.md 불변 규칙)

- **파괴적이지 않으면 연다.** 1인·서버 없음·원격 없음 프로젝트에서 승인 창이 지켜 줄 "남의
  변경"이 없다. 사용자는 커밋·테스트·빌드·설치·조사에 관여하지 않는다 — 창이 뜨면 결함이다.
- **트레이드오프**: "승인한 것만 돌았다"는 보장 대신 git 이력·작업 로그가 그 역할을 한다.
- **남기는 것** (사람이 본다):
  - 파괴적 명령 — 삭제류·`git reset --hard`·`git push --force`·`git clean`·`git checkout --`.
    `settings.json` deny에 셸 도구별로 둘 다 있다. allow 와일드카드로 덮이지 않게 한다.
  - 패키지 **추가** = 잠금 파일에 없는 새 의존성(`pip install <새 패키지>`·`flutter pub add`·
    `npm install <패키지>`) — 공급망. 잠금 파일대로 **설치**하는 `pip install -r`·`pub get`·`npm ci`는
    추가가 아니라 연다(2026-09-05 구분 확정, CLAUDE.md 불변 규칙).
  - 다운받은 실행물(스크립트·바이너리)의 실행 — 데이터 다운로드와 구분한다.
- **못 없애는 것**: `.claude/`·`.git/` 안의 Write/Edit는 규칙과 무관하게 항상 묻는다(보호
  디렉토리 `[H12]`). 프로젝트 밖 디렉토리 읽기는 세션당 한 번 묻는다 `[H8]`. 그냥 받는다.
- **승인 창이 반복되면 처방 순서**: ① 이미 열린 형태로 부르고 있나(습관 — 반복되면 훅)
  ② 규칙 추가. kanadic 최대 원인 154회는 규칙이 아니라 습관이었다 `[H1]`. hanmunstudy에서도
  `git add -A`가 열려 있는데 `git add <경로>`로 불러 물었다 `[H12]`.
- 종전 기준 "빈도 × 위험, 상태 변경은 묻는다"는 **폐기** `[H12]`.

## 2. 병목 측정법

- `scripts/measure_wait.py` — tool_use↔tool_result 간격으로 **호출별 벽시계**. `--grep <명령>`으로
  같은 호출의 전후를 비교한다(절대값에는 명령 자체 시간이 섞인다 `[H2]`).
- `scripts/measure_approvals.py` — 원인 **분류**. 예측이며 양쪽으로 틀린 실사례가 있다 `[H2]` —
  어디를 볼지 고르는 데만 쓴다. 규칙 문법이 하네스와 이 분석기 두 곳에 있으므로 어긋나면
  보고서가 거짓말을 한다 — 대조 테스트가 있다 `[H10]`.
- 승인 대기는 상한이 없다(실행 12초가 벽시계 156초 `[H2]`).
- **"창이 떴는가" 판정법** — 한 가지로 단정하지 않는다:
  - 벽시계만으로는 못 가른다(사용자가 2초 만에 누르면 정상 실행과 같다 `[H5]`).
  - `settings.local.json`에 **이번에 새로** 적혔는가 — "항상 허용"을 눌렀다면 반드시 적힌다.
    단 **"한 번만 허용"은 아무것도 안 남긴다** `[H12]`.
  - **사용자가 옆에 있으면 묻는 것이 가장 확실하다.**
- 호출이 0건으로 나오면 '깨끗함'이 아니라 실패다 — 트랜스크립트 형식·경로부터 본다.

## 3. 허용 규칙 작성법

1. **규칙을 추측하지 않는다.** 사용자가 "다시 묻지 않기"를 한 번 고르게 한다 — 매칭하는 그
   코드가 `settings.local.json`에 정확한 패턴을 적는다 `[H3]`.
2. **효력이 있는 것은 `settings.local.json`뿐이다.** `settings.json`의 `allow`는 이 환경에서
   안 걸린다(`hooks`·`env`·`deny`는 산다 — deny는 파괴적 명령을 던져야 시험되므로 확인하지
   않았고 지우지도 않는다) `[H6]`. local은 gitignore라 공유 불가 — 새 클론은 §5 스타터로
   시작한다. 그래서 템플릿 `settings.json`의 allow는 비어 있다.
3. **규칙 문법**:
   - 와일드카드는 작동한다. **패턴에 역슬래시가 있으면 죽는다** — 경로는 슬래시로 부르고
     슬래시로 등록한다 `[H6]`.
   - `X*`(공백 없이)는 `X`와 `X <인자>`를 함께 받는다 `[H10]`.
   - **`X:*`(콜론+별표)는 `X *`와 동등하다** — 공식 문법이고 패턴 끝에서만 와일드카드다(가운데 콜론은
     리터럴). 승인 창은 공백 형태를 적지만 손으로 쓴 규칙·다른 프로젝트에서 온 규칙은 이 형태가 흔하다.
     분석기가 이걸 몰라 거짓 보고서를 낸 실사례 `[H15]`. 출처: code.claude.com/docs/en/permissions.
   - `Bash(...)`와 `PowerShell(...)`은 별개다 — **양쪽에 넣는다** `[H1]`.
   - 한글(비ASCII)이 든 명령을 "다시 묻지 않기"로 열지 않는다 — 파일명이 깨져 적힌다
     `[H9]`. 열어야 하면 ASCII 경로로 부른다. (단 `git add *`처럼 와일드카드 규칙이 있으면
     한글 인자도 걸린다 `[H12]`.)
   - 자동 승인이 위험한 명령은 인자를 필수로 건다(`dart format *` — 맨몸 전체 포맷은 묻는다).
   - `deny`가 `allow`보다 우선한다. deny도 셸 도구별로.
4. **일회성 명령을 열지 않는다** — 다시 부를 일이 없어 이득 0, 권한만 남는다 `[H9]` `[H11]`.
   세션 UUID·스크래치패드 절대경로·인라인 파이썬이 박힌 규칙이 전형이다. 호출 형태로 푼다(§4).
5. **규칙이 걸리는지 검증할 때 읽기 전용 명령을 쓰지 않는다.** `git status`·`git log`류는
   규칙과 무관하게 자동 허용처럼 동작해 어떤 규칙을 넣어도 "걸렸다"가 나온다 `[H5]`.
   **실제로 묻던 명령으로 검증한다.** ⚠️ 단 "자동 허용 부류"를 단정하지 말 것 — 자동 허용으로
   보였던 `Set-Location`이 다른 환경·시점에선 9건을 물었다 `[H14]`. **물면 규칙을 넣는다.**
6. **규칙은 즉시 걸린다 — 재시작을 기다리지 않는다** `[H14]` (local에 넣자마자 같은 세션의
   다음 보고서에서 사라짐. [H4]·[H5]의 "미적용"·"갈렸다" 오진은 죽은 settings.json에 넣었거나
   벽시계로 판정한 것이 원인). 그 자리에서 실제로 묻던 명령으로 재검증하고, 판정은 §2 판정법.
7. **audit 때 local을 청소한다 — 넓히는 게 아니라 줄이는 작업이다** `[H14]`. 지난 세션의
   스크래치패드 경로, 다른 저장소 경로, 깨진 한글, 훅이 막는 형태의 규칙은 죽은 규칙이다
   (실사례: 169개 → 64개).
8. **규칙을 닫을 때는 두 파일을 함께 본다** — local에 같은 규칙이 있으면 그쪽이 계속 연다 `[H6]`.

## 4. 호출 형태 — 한 호출 = 한 명령, 앞에 무엇도 붙이지 않는다

| 피할 형태 | 왜 | 대신 |
|---|---|---|
| `cd X && cmd` / `Set-Location X; cmd` | 접두사 규칙에 안 걸리거나(Bash), 앞머리 하나로 뒤까지 통째로 통과해 승인 이력을 못 믿게 된다(PowerShell) `[H1]` `[H7]` | `cd` 따로, 명령 따로. cwd는 호출 사이에 유지된다 |
| `FOO=bar cmd` / `$p = "…"; cmd` | 리터럴로 시작하지 않아 allow 규칙에 안 걸린다 `[H1]`. 공식 문서: **알려진 안전 변수**(`NODE_ENV` 등) 대입만 매칭 전에 벗겨지고 그 외 변수는 안 걸린다(deny는 어떤 대입도 넘어 매칭) `[H15]` | `settings.json`의 `env` |
| `& (Join-Path …)` 식 표현식 시작 | 같은 이유(77.8초) `[H6]` | 전체 경로를 글자 그대로, 그 리터럴로 규칙 등록 |
| `& "<경로>"` (호출 연산자+따옴표) | 규칙은 맨몸을 전제 — 같은 adb devices가 27.6초 vs 2.2초 `[H14]` | 경로에 공백이 없으면 `&` 없이 맨몸으로 |
| **쓰는** `git -C <경로>` (add·commit) | 규칙이 `git add *` 형태를 전제(21건 147초) `[H6]` `[H14]` | 루트에서 맨몸으로. **읽는** git(status·log·diff·show)의 `-C`는 규칙을 넣어 열 수 있다 — cwd 드리프트 확인용(`[H13]`) |
| `A; if ($?) { B }` 복합문 | 어느 규칙에도 안 걸린다(4건) `[H14]` — [H7]의 "앞머리 통과"와 갈리는 관측: 하네스 동작이 바뀌었을 수 있다. 어느 쪽이든 처방 동일 | 두 번에 나눠 부른다 |
| 공백 없는 경로를 따옴표로 | `git commit -F "경로"`가 `-F *`에 안 걸림(18.5초) `[H6]` | 따옴표 없이 |
| `cmd \| Select-String` / `2>&1` | PowerShell에선 꼬리가 검사되지 않고, 자르려다 3~7배 `[H7]` `[H8]` | 그냥 받는다 — 큰 출력은 하네스가 파일로 떨궈 준다 |
| `python -c "…"` | 이스케이프째 박제되는 죽은 규칙 `[H11]` | 스크립트 파일 |
| 스크래치패드 절대경로 | 세션 UUID가 박혀 다음 세션에 안 걸린다 `[H11]` | 경로는 스크립트 안에서 계산 |
| 셸에서 환경변수 확인 | 일회성(82.3초) `[H11]` | 스크립트 안에서 `os.environ` |
| 셸 `for/do/done` | 규칙에 안 걸린다 | 파이썬 |

- **★ 앞머리 통과를 우회 수단으로 쓰지 않는다.** 허용된 명령을 앞에 붙여 뒤엣것을 통과시키면
  사용자는 자기가 승인한 것만 돌았다고 오해한다. 명령은 있는 그대로 부르고, 물으면 물리는
  대로 둔다. 속도보다 이쪽이 우선이다 `[H7]`.
- **작업 디렉토리**: 트리 안 `cd`는 유지되고, 트리 밖은 거부·루트로 리셋된다 `[H5]`. 유지되기
  때문에 **드리프트** 위험이 있다 — 하위에서 루트용 명령을 부르면 에러 없이 조용히 다른 일을
  한다(`pytest tests/` 0건 통과, `git add -A` 하위만). 하위에 들어갔으면 끝나자마자 돌아온다.
  100% 믿지도 않는다 — 중간에 초기화된 실측이 있다. "찾을 수 없다"류로 죽으면 cwd부터 본다.
  **Bash·PowerShell 도구는 cwd를 공유하고, 승인된 추가 디렉토리로의 `cd`는 트리 밖이어도
  유지된다** `[H13]` — 다른 저장소에서 작업한 뒤 `git add -A`를 부르면 그쪽이 담긴다.
  다른 저장소에 들어갔으면 **돌아오기 전에 `git status`로 어느 저장소인지 본다.**
- **여러 줄 커밋 메시지**: here-string·인라인으로 넘기지 않는다(`pathspec` 오류). 저장소 루트
  `.commit_msg.txt`(gitignore)에 쓰고 `git commit -F .commit_msg.txt`. 경로를 고정해야 규칙
  한 줄로 영구히 끝난다. `.git/` 안에 두지 않는다(보호 디렉토리, 137초) `[H8]`.

## 5. settings.local.json 스타터

hanmunstudy(Flutter·Windows) 실물(2026-08-22) 기준이다. 초기화 때 `.claude/settings.local.json`에
붙여 넣고(없으면 만든다), **이 스택에 없는 줄은 뺀다.** 전역 `python` 기준 — 가상환경이면
`.venv/Scripts/python.exe`처럼 슬래시 경로로 바꾼다. `additionalDirectories`의 프로젝트 폴더명은
`~/.claude/projects/` 아래 실제 이름으로(경로의 `:`·`\`가 `-`로 바뀐 형태).

```json
{
  "permissions": {
    "allow": [
      "WebSearch",
      "WebFetch",
      "Write(.commit_msg.txt)",
      "Edit(.commit_msg.txt)",
      "PowerShell(git add *)",
      "PowerShell(git commit -F .commit_msg.txt*)",
      "PowerShell(Set-Location *)",
      "Bash(git add *)",
      "Bash(git commit -F .commit_msg.txt*)",
      "Bash(cd *)",
      "PowerShell(flutter test)",
      "PowerShell(flutter test --reporter failures-only)",
      "PowerShell(flutter analyze)",
      "PowerShell(flutter doctor)",
      "PowerShell(flutter devices)",
      "PowerShell(flutter pub get)",
      "PowerShell(flutter pub outdated)",
      "PowerShell(flutter clean)",
      "PowerShell(flutter build apk*)",
      "PowerShell(dart analyze)",
      "PowerShell(dart format *)",
      "PowerShell(python -m pytest *)",
      "PowerShell(python scripts/*)",
      "Bash(python -m pytest *)",
      "Bash(python scripts/*)",
      "PowerShell(C:/Users/user/AppData/Local/Android/Sdk/platform-tools/adb.exe devices)",
      "PowerShell(C:/Users/user/AppData/Local/Android/Sdk/platform-tools/adb.exe install -r *)",
      "PowerShell(C:/Users/user/AppData/Local/Android/Sdk/platform-tools/adb.exe logcat *)"
    ],
    "additionalDirectories": [
      "C:/Users/user/.claude/projects/C--Users-user-Documents-<프로젝트폴더명>"
    ]
  }
}
```

- `flutter test *`는 안 연다 — 개별 실행은 훅이 막고, 맨몸과 failures-only면 충분하다.
- `Set-Location`/`cd` 규칙을 넣는 이유: cd 훅이 권장하는 대체 수단인데 자동 허용이 아닌
  환경에서 매번 물었다(9건 실측 `[H14]`). 단독 cd는 파괴적이지 않다.
- `flutter pub *`가 아니라 `get`·`outdated`만 — `pub add`는 패키지 추가(§1 남기는 것).
- `Edit(.claude/**)`류는 넣지 않는다 — 죽은 규칙이다 `[H12]`.
- ⚠️ `additionalDirectories`는 문법만 적었고 **실측 전**(2026-08-22) — 다음 `/approval-audit`에서
  디렉토리 승인이 뜨는지로 판정한다.
- 붙여 넣은 뒤 실제로 묻던 명령으로 확인한다(§2 판정법).

## 6. 훅 — 문서로 안 막히면 구조로

문서에 두 곳 적힌 함정이 한 세션에서 154회 반복됐다 `[H1]`. 반복되는 호출 습관은 PreToolUse
훅으로 막는다. 4종이 동봉·등록돼 있다(`settings.json`의 hooks, matcher `Bash|PowerShell` 한 블록):

| 훅 | 막는 것 |
|---|---|
| `no_redundant_cd` | `cd <저장소 루트> &&\|; …` 접두사 |
| `no_output_filter` | `2>&1`·`\| Select-String/Select-Object` 출력 자르기 `[H8]` |
| `no_targeted_flutter_test` | `flutter test <인자>` — 전체(23~27초)가 파일 하나(158~206초)보다 싸다. Flutter 없으면 세 곳(훅 파일·settings.json 항목·`tests/test_no_targeted_flutter_test.py`)에서 삭제 — CLAUDE.md 초기화 5번 |
| `no_inline_python` | `python -c "…"` `[H11]` |

규범 (전부 실측에서 나온 것):
- **양쪽 셸에 건다.** 한쪽에만 걸면 반대쪽으로 그대로 통과한다 `[H11]`.
- **등록 직후 ① 막히는 명령 ② 정상 명령을 둘 다, Bash·PowerShell 양쪽에서 실제로 부른다.**
  통과하는 쪽만 보면 죽은 훅과 정상 훅의 증상이 완전히 같다 `[H11]`.
- 훅은 재시작 없이 즉시 적용된다 `[H11]`(한 번 뒤집혔다 복구된 결론 — 이력 참조).
- deny 출력은 반드시 `hook_io.deny()`로 낸다. `json.dumps(..., ensure_ascii=False)`를 직접 쓰면
  cp949 콘솔에서 죽어 훅이 통째로 무력화된다 `[H11]`. 훅 회귀 테스트는 `PYTHONIOENCODING`을
  cp949로 고정해 돌린다(`tests/test_hook_io.py`) — 현재 환경을 물려받으면 아무것도 검증하지 않는다.
- 훅 경로: 템플릿은 `$CLAUDE_PROJECT_DIR`로 두었다. **환경에 따라 다르다** — 빈 값인 환경이 있었고
  `[H11]`, 데스크톱 앱(2026-09-06)에서는 채워져 있었다 `[H15]`. 단정하지 말고 **등록 직후 막히는
  명령을 불러 확인**하고, 안 막히면 절대 경로(슬래시)로 바꾼다.
- **훅 테스트에서 자식 파이썬의 인코딩을 물려받지 않는다.** PowerShell 도구 환경에는
  `PYTHONIOENCODING=utf-8`이 있고 Bash 도구 환경에는 없다 — 같은 테스트가 셸에 따라 갈린다
  `[H13]`. 인코딩을 검증하는 테스트는 cp949로 고정, 그 외는 utf-8로 고정한다.
- 반례를 테스트에 넣는다(맨몸·정상 호출까지 막으면 고칠 방법이 없다). 재보지 않은 것을 막지
  않는다(YAGNI). 훅이 고장 나면 막지 않고 통과시킨다.
- 그 형태가 정말 필요하면 사용자에게 말하고 승인을 받는다. **훅을 우회하지 않는다.**
- **예외는 "문자열이 고정인가"로 연다.** `flutter test --reporter failures-only` 한 형태만 훅
  예외 + 정확 일치 규칙. 가변으로 열면 다시 매번 달라진다. 연 예외의 반례를 테스트에 넣는다.
- **효과 검증은 "호출 형태가 바뀌었나"로 본다** `[H8]` — "작업이 쉬웠나"가 아니다.

---

# 2부 — 실측 이력 (날짜순)

> 결론이 뒤집힌 것도 지우지 않는다. 각 항목: 무엇을 봤고 → 무엇을 결론냈고 → (뒤집혔으면) 왜.

## [H1] kanadic 2026-08-18~19 — 최대 원인은 규칙이 아니라 호출 습관
- 50세션에서 `cd <루트> && …` 223회, 한 세션에서 154회. 문서에 두 곳 적혀 있었는데도 반복됐다
  → 문서로 안 막히는 습관은 훅으로(§6).
- Bash 규칙만 있는 상태에서 PowerShell 호출 733회가 전부 물었다 → 규칙은 도구별.
- `curl.exe *` 규칙이 있는데도 변수 대입 접두사를 붙여 물었다 → 규칙이 아니라 형태의 문제.
- 23.4분 세션에서 테스트 총합 30초, 승인 대기 약 6분. 테스트 389건을 반으로 줄여도 6초 절약.

## [H2] hanjadic 2026-08 — 측정 도구의 한계
- `measure_wait.py --grep pytest`: 52.2초 → 36.7초, 실행 자체 32초 — 차이가 대기였다.
- 실행 12초짜리 테스트가 벽시계 156.3초.
- `measure_approvals.py`는 git 과대·자기 자신 과소로 양쪽으로 틀렸다 → 예측으로만 쓴다.

## [H3] 추측한 규칙으로 두 번 틀림
- 규칙을 손으로 추측해 넣은 것이 두 번 안 걸렸다 → "다시 묻지 않기"가 적어 주는 것이 정답.

## [H4] kanadic 2026-08-18 — "도중 수정 미적용" 오진에 한나절
- 규칙을 고쳤는데 안 걸려 "재시작해야 적용된다"로 판단. 이후 [H5]·[H6]으로 원인이 다른
  곳에 있었음이 드러남. 현재 결론: 단정하지 않는다(§3-6).

## [H5] hanjadic 2026-08-20 — 세 번 뒤집힌 자리
- "즉시 걸린다"(36.3→4.3초)고 결론 → **같은 날 취소**: 원인은 규칙이 아니라 사용자의 "항상
  허용" 클릭이었다. 벽시계만으로는 창이 떴는지 못 가른다.
- 실험에 `git status`·`git log`를 써서 어떤 규칙을 넣어도 "걸렸다"가 나왔다 — 읽기 전용은
  규칙과 무관하게 자동 허용.
- "와일드카드 무효, 정확 일치만"(양 도구)이라 결론 → [H6]에서 취소(효력 없는 settings.json
  위의 실험이라 아무것도 증명하지 못했다). 같은 날의 "allow 101→35 정리"도 실질 손해·이득 없음.
- 작업 디렉토리: "매 호출 리셋"이라는 틀린 일반화(트리 밖에서만 뜨는 리셋 메시지를 전체
  규칙으로 착각) 위에 "복합 명령용 규칙을 열자"는 잘못된 제안까지 갔었다 → 실측: 트리 안
  `cd app`은 유지, 트리 밖은 거부·리셋("Shell cwd was reset to…").
- 메타 교훈: 규칙을 손대기 전에 "이 규칙이 어느 파일에 있나"부터 본다.

## [H6] hanjadic 2026-08-20~22 — settings.json allow는 효력이 없다
- 세션 시작 전부터 settings.json에 있던 `PowerShell(flutter devices)`가 물었고 직후 local에 새로
  적혔다. 같은 파일의 `hooks`·`env`는 살아 있었다(훅이 실제로 막음).
- local에 `PowerShell(flutter pub *)` → `pub get`·`pub --help` 둘 다 안 물음(와일드카드 작동).
  `.venv\Scripts\python.exe -m pytest *`는 물었고 슬래시로 바꾸자 안 물음(역슬래시 패턴 사망).
- 규칙을 닫았는데 local에 같은 규칙 8건이 더 있어 계속 열렸다.
- `git -C <경로>`는 규칙을 넣어도 물었다. `git commit -F "경로"`(따옴표)는 `-F *`에 안 걸려
  18.5초(다른 git은 0.7초). `& (Join-Path …)` 시작은 77.8초(실행 2~5초).

## [H7] hanjadic 2026-08-20 — 파이프·복합의 실제 검사 방식 (PowerShell 도구)
앞머리를 `git log`로 고정하고 꼬리만 바꿈:

| | 명령 | 결과 |
|---|---|---|
| T1 | `git log \| Select-String …` | 안 물음 |
| T2 | `git log \| Out-File <경로>` | **안 물음** (파일을 쓰는 cmdlet인데도) |
| T3 | `git log --oneline -1; Copy-Item …` | **안 물음** |
| T4 | `Copy-Item …` (혼자) | **물음** |

- T3·T4가 갈린다 → PowerShell 도구에서는 앞머리가 줄 전체를 결정하고 `|`·`;` 뒤는 검사되지
  않는다. 파이프 꼬리용 규칙은 넣어도 효과가 없다("관측되지 않는 값").
- 종전 서술 "복합 명령은 접두사 규칙을 못 통과해 매번 묻는다"가 정정됨 — 피하는 규칙은 유지,
  이유가 "안 걸려서"에서 "통째로 걸려 버려서"로 바뀜.
- 같은 날 Bash 도구의 `;` 복합은 앞머리가 규칙에 없자 그대로 물었다 — 도구 간 일반화 금지.
- hanmunstudy 2026-08-22 재확인: `git status --short; git log --oneline -1` 안 물음(T3과 일치).

## [H8] hanjadic 2026-08-19~20 — 기타
- `.commit_msg.txt`를 `.git/` 안에 두자 `Write`마다 137.1초(일반 위치 0.1초). 루트 고정 +
  gitignore로 옮기고 사용자 확정("안 떴다", 2026-08-19). 두 번 옮긴 끝의 결론.
- 출력을 자르려고 `| Select-String`·`2>&1`을 붙이자 209초 vs 맨몸 27.4초(3~7배).
- 프로젝트 밖 디렉토리(`~/.claude/projects/`) 읽기는 세션당 한 번: 첫 건 6.2초, 이후 9건 0.4초.
- 훅 도입 뒤 한 기능이 확인 0건 — "간단해서였나" 의심 → 세어 보니 파일 지정 호출 23회는 전부
  훅 이전, 그 기능은 맨몸 15회만. 호출 형태가 달라진 것.

## [H9] hanmunstudy 2026-08-22 (초기화) — 한글 경로 규칙이 깨져 적힘
- `Remove-Item "docs/playbooks/팩_MCP.md"`를 "다시 묻지 않기"로 승인하자 local에
  `PowerShell(Remove-Item "docs/playbooks/��_MCP.md" …)`로 적혔다. 역슬래시와 별개 결함.
- 게다가 일회성 삭제 호출이라 이득 0, 삭제 권한만 남았다 → 삭제.

## [H10] hanmunstudy 2026-08-22 (Phase 001 approval-audit) — 분석기 문법 드리프트
- 규칙을 `X`+`X *` 두 줄에서 `X*` 한 줄로 합쳤더니 `measure_approvals.allowed()`가 그 규칙을
  통째로 못 보고 `git commit` 13건을 '물었을 것'으로 셌다(실제로는 안 물었다). 대조 테스트 추가.
- `X*`가 하네스에서 작동하는 것은 별도 확인(맨몸 호출이 새 규칙을 안 만들었다).
- Phase 001에서 일회성 규칙 31건 중 20건이 재사용 불가라 삭제.

## [H11] hanmunstudy 2026-08-22 — 훅 4종이 전부 죽어 있었다
- 벽시계 71건/354초, 8초 초과 7건이 238초(67%). 상위 2건 143초(60%)는 환경변수 한 번 보려는
  `"ps: IOENC=[$env:PYTHONIOENCODING] …"` 82.3초와 같은 것을 Bash에서 60.9초. 스크래치패드
  절대경로 스크립트 3건 21.5·11.8·10.2초. echo 한 건은 "항상 허용"으로 세션 UUID가 박힌 죽은
  규칙을 만들었다(그 자리에서 삭제). CLAUDE.md가 금지한 두 항목을 같은 세션에서 다시 밟음.
- 새 훅 `no_inline_python` 등록 직후 `python -c "print(1)"`이 그대로 실행됨. 세션 시작 전부터
  있던 `no_output_filter`도 `git status --short 2>&1`을 안 막음 → 새 등록 문제가 아니라 훅이
  아예 안 돌았다. `$env:CLAUDE_PROJECT_DIR` 빈 값 — 절대 경로로 바꿔도 여전히 안 막음.
  → 종전 결론 "훅은 즉시 적용된다"에 ⚠️를 달았었다.
- **후속 세션에서 원인 확정**: `json.dumps(..., ensure_ascii=False)`가 cp949 콘솔에서 `—`(U+2014)
  를 못 찍어 `UnicodeEncodeError` exit 1. 하네스는 훅이 죽으면 통과시키므로 4개 전부 조용히
  무력화. deny 경로에서만 죽어 "정상 호출 통과" 테스트는 계속 초록불. `blocked()` 24건 통과도
  함수만 본 것(배선과 별개). **"즉시 적용"은 맞았다 — 결론 복구.**
- 초록불은 실력이 아니라 환경변수 덕: pytest를 부르는 PowerShell 도구 환경에
  `PYTHONIOENCODING=utf-8:surrogateescape`가 있어 서브프로세스도 UTF-8이었고, 하네스가 훅을
  띄울 때는 없다. → 회귀 테스트는 cp949 고정. 처방은 `ensure_ascii` 유지(`\uXXXX`는 항상 ASCII,
  하네스가 JSON을 파싱하므로 한글 안내는 그대로 보인다 — 되돌린 뒤 라이브 2건 한글 그대로 차단).
- matcher가 `no_redundant_cd`만 Bash, 나머지 셋은 PowerShell에만 → Bash `python -c "print(1)"`
  통과. `Bash|PowerShell`로 확장(정규식 교대 실측 확인). deny 출력을 `hook_io.py` 한 곳으로
  모음(복사본 4벌이 같은 버그를 4번).

## [H12] hanmunstudy 2026-08-22 — 원칙 전환과 동석 실측
- 사용자 동석으로 호출 6건:

  | 호출 | 승인 창 |
  |---|---|
  | `git status --short; git log --oneline -1` | 안 뜸 |
  | `git add docs/playbooks/노하우_….md` (한글 경로, `git add *` 규칙) | 안 뜸 |
  | `python -m pytest tests/ -q` | 안 뜸 |
  | `.claude/settings.local.json` Edit ×2 (`Edit(.claude/**)` 있음) | **뜸** |
  | `.claude/skills/approval-audit/SKILL.md` Edit | **뜸** |

  같은 세션에서 `.claude/` 밖 Edit(CLAUDE.md·docs/)은 한 건도 안 물음 → `.claude/`는 `.git/`과
  같은 보호 디렉토리. `Edit(.claude/**)` 제거.
- 판정법 사각지대: 사용자가 "한 번만 허용"을 눌러 local에 새 줄이 없었고, 그것을 "안 물었다"로
  오판했다. 사용자가 있으면 묻는다.
- 직전 커밋에서 `git add -A`가 열려 있는데 `git add <경로>`로 불러 물었다 — 습관 범주.
- **원칙 전환(사용자 결정)**: 종전 "허용 목록을 넓히지 않는다 / 상태 변경은 묻는다"(템플릿
  기본값, kanadic·hanjadic에서 유지)는 1인 프로젝트에서 커밋·테스트·빌드마다 사용자를 불러냈고
  `/approval-audit`은 설계상 그 창을 없앨 수 없었다 → "파괴적이지 않으면 연다"로 교체(§1).
  local allow에 `git add *`·`flutter build apk*`·`python scripts/*`·`dart format *`·adb logcat,
  Bash 쪽 동등 규칙, `additionalDirectories` 추가. `flutter pub *`는 `get`·`outdated`로 좁힘.

## [H13] 템플릿 역이식 2026-08-22 — 훅 테스트가 셸 도구에 따라 갈렸다
- hanmunstudy의 훅·테스트를 템플릿으로 옮기고 **Bash 도구**에서 pytest를 돌리자 "깨진 json"
  테스트 2건이 `TypeError: argument of type 'NoneType'`으로 실패. 같은 테스트가 hanmunstudy
  **PowerShell 도구**에서는 132건 전부 초록이었다.
- 원인: 테스트가 자식 훅의 stderr를 `encoding="utf-8"`로 디코드하는데, 자식의 stderr 인코딩은
  환경을 물려받는다. PowerShell 도구 환경에는 `PYTHONIOENCODING=utf-8:surrogateescape`가 있고
  Bash 도구 환경에는 없어 cp949로 나와 디코드 실패 → stderr `None`. [H11]과 같은 뿌리
  ("초록불이 환경변수 덕")가 테스트 쪽에도 있었다.
- 처방: 그 테스트 3건에 `env={**os.environ, "PYTHONIOENCODING": "utf-8"}`를 명시. 인코딩 회귀는
  `test_hook_io.py`가 cp949 고정으로 따로 본다. 고친 뒤 Bash·PowerShell 양쪽 132건 통과.
- 덤: 역이식 검증 중 `python -m pytest … 2>&1 | tail -5`를 불렀다가 `no_output_filter` 훅에
  막혔다 — hanmunstudy 훅이 살아 있다는 라이브 증거.
- **cwd 드리프트 실사례**: Bash 도구로 `cd <템플릿> && pytest`를 부른 뒤 PowerShell 도구로
  `git add -A`를 부르자 **템플릿 저장소가 스테이징됐다.** 두 도구가 cwd를 공유하고, 승인된
  추가 디렉토리(`request_directory`로 연 곳)로의 `cd`는 트리 밖이어도 유지된다 — 승인 전에는
  "Shell cwd was reset"으로 거부됐던 같은 경로다. 이번엔 스테이징 내용이 의도와 같아 그대로
  커밋했지만, 반대였으면 엉뚱한 저장소에 커밋할 뻔했다.

## [H14] hanjadic 2026-08-29 — 최대 원인은 또 호출 형태였다 (셸 343건 / 2,110초)

- 측정기 분류는 "형태 5회 / 분류 밖 84회"라 했지만 `measure_wait` 실측 상위는 전부 호출
  형태였다 — 각주의 "자기 자신은 과소" 그대로. **분류 상위보다 measure_wait 느린 순을 먼저 본다.**
- `& "<adb 경로>"` 15건 181초 — 규칙은 맨몸 adb.exe 전제. 같은 `adb devices`가 27.6초와
  2.2초로 갈렸다(명령 자체는 2초). → 경로에 공백이 없으면 `&` 없이 맨몸.
- 쓰는 `git -C "<루트>"` 21건 147초 — 규칙은 `git add *` 형태 전제. → 쓰는 git은 루트에서
  맨몸, 읽는 git(status/log/diff/show)의 `-C`만 규칙으로 열었다(사용자 확인 후 — cwd 드리프트
  때 다른 저장소 확인용, [H13]과 연결).
- `flutter analyze; if ($?) { … }` 복합문 4건 — 어느 규칙에도 안 걸림. [H7](앞머리 통과)과
  갈리는 관측 — 하네스 동작 변화 가능성. 처방은 동일(복합문 금지)이라 재실험은 안 했다.
- `Set-Location` 9건 — cd 훅이 권장하는 대체 수단인데 규칙이 없어 매번 물었다([H5]의 "자동
  허용" 관측과 갈림) → 규칙 추가(사용자 확인 후).
- **★ 규칙은 즉시 걸린다 — 확정.** 규칙을 넣자마자 같은 세션의 다음 보고서에서 Set-Location이
  사라졌다. 측정기는 2026-08-20부터 맞게 적고 있었고 스킬 문서만 뒤처져 있었다.
- local 청소 169 → 64: 지난 세션 스크래치패드 경로·다른 저장소·깨진 한글·훅이 막는 형태.
- flutter test 42~53초 / build 42~72초 / 첫 analyze 75초(실제 64초, 웜업)는 **진짜 명령
  시간**이라 줄일 것이 없다 — 승인 대기와 구분할 것.
