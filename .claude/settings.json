{
  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": [],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force*)",
      "Bash(git push -f*)",
      "Bash(git reset --hard*)",
      "Bash(git clean *)",
      "Bash(git checkout -- *)"
    ]
  }
}
