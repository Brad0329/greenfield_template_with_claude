# <프로젝트명> — <시스템 한 줄 설명>

<기술 스택 한 줄>로 처음부터 만드는 신규 시스템.

<!-- ════════════════════════════════════════════════════════════════
     ★★★ 템플릿 초기화 모드 (초기화가 끝나면 이 블록을 삭제한다) ★★★
     이 파일에 아직 `<...>` 플레이스홀더가 남아 있으면 = 갓 복사된 미초기화 템플릿이다.
     이 상태에서 사용자가 첫 인사를 하면, 다른 작업에 앞서 아래 체크리스트를
     하나씩 확인시키는 것으로 시작할 것. 답을 받아 실제로 파일을 고친 뒤 다음 항목으로.
     ════════════════════════════════════════════════════════════════

## ★ 초기화 체크리스트

1. **`.claude/` 복사 확인** — 숨김 폴더라 누락되기 쉽다. 없으면 사용자에게 알린다.
2. **프로젝트 정체성** — 이름 / 한 줄 설명 / 기술 스택을 물어 이 파일의 `<...>`를 채운다.
3. **버전 관리** — `git init` 여부를 묻는다. 원격 저장소는 **필수가 아니다** —
   필요한지(배포·백업·협업) 물어보고, 필요 없으면 그대로 진행한다.
4. **`.gitignore`** — `.claude/settings.local.json`·`.env`·비밀정보·빌드 산출물이 들어 있는지.
5. **★ 스택 기본값 채우기 — 이 단계를 건너뛰지 말 것**
   - `.claude/settings.json`의 `permissions.allow`는 **비어 있다.** 이 프로젝트가 실제로 쓸
     명령(테스트·빌드·실행)을 넣는다. **틀린 기본값은 빈 칸보다 나쁘다** — 안 맞는 허용 목록은
     매 명령마다 사용자에게 확인을 묻게 만든다.
   - 아래 '테스트 규칙'과 '배포 체크리스트'의 `<...>`를 이 스택의 실제 명령으로 채운다.
6. **이 환경에서 무엇이 되는지 확인** — 서브에이전트 호출, `/code-review`·`/security-review` 같은
   스킬이 실제로 동작하는지. 안 되면 '검증 게이트'의 대체 수단으로 간다.
7. **`docs/REQUIREMENTS.md` / `docs/SCHEMA.md` / `work_log/plan.md`** — 지금 채울지,
   Phase 001에서 채울지 정한다.
8. **`README.md` 삭제** — 템플릿 사용법 문서라 실제 프로젝트엔 불필요. 지울지 확인.
9. 끝나면 **이 초기화 블록을 삭제**하고 정상 모드로 전환한다.
<!-- ════════════════ 초기화 블록 끝 ════════════════ -->

## 현재 단계
- 전체 Phase 체크리스트/아키텍처의 **단일 원본 = `work_log/plan.md`** (여기에 중복 기재 금지)
- 현재: **Phase <N> — <이름>** (Phase 완료 시 이 줄과 plan.md 체크박스를 함께 갱신)
- 권장 Phase 골격: 요구사항/설계 확정 → 프로젝트 세팅 → **MVP 코어(가장 위험한 것 먼저)** →
  기능 확장 → 공개 준비 → 운영

## 새 세션 시작 시
1. `work_log/plan.md` — 전체 현황·아키텍처·완료 Phase
2. 진행 중 Phase의 `work_log/Phase_XXX.md` — 이전 결정과 실패 경험을 이어받는다

## 요구사항 정책 ★"정답 기준"이 없으면 완료를 판단할 수 없다
- 기능 요구사항 + 수용 기준의 **단일 원본 = `docs/REQUIREMENTS.md`**
- **구현 전**: 그 기능의 수용 기준이 REQUIREMENTS.md에 있어야 한다. 없으면 먼저 스펙화하고
  모호점은 사용자에게 질문한 뒤 시작한다.
- **요구사항이 바뀌면 즉시 갱신** — 대화 속 합의로만 남기지 말 것(다음 세션이 구버전으로 구현한다).
- **수용 기준은 검증 가능한 문장으로 쓴다.** "잘 동작한다"가 아니라 "X를 넣으면 Y가 N건 나온다".
  그래야 그대로 테스트 케이스가 된다.
- **YAGNI**: REQUIREMENTS.md에 없는 요구사항을 위한 추상화·설정화·일반화 금지.
- **문서가 커지면 도메인별로 분할**하되 REQUIREMENTS.md는 목차+링크 인덱스로 유지한다.
- **요구사항 ID 대장 — 새 번호는 여기서 받아간다.** 아이디어 단계여도 번호를 잡았으면 적는다.
  **번호를 재사용하지 말 것.**

  | ID | 기능 | 상태 |
  |---|---|---|
  | F-001 | <첫 기능> | 미착수 |
  | **다음 번호** | **F-002** | |

## 데이터 모델/스키마 변경 게이트 ★스키마는 되돌리기 비싸다
- 스키마 **결정의 기록 = `docs/SCHEMA.md`** (설계 의도, 관계, 제약, 변경 이력).
- **AI는 임의로 컬럼 추가/변경, 마이그레이션 수정을 하지 않는다.**
  ① 변경안(무엇을·왜·영향 범위)을 SCHEMA.md에 먼저 기록 → ② 사용자 확인 → ③ 반영.
- **SCHEMA.md는 "결정과 이유"를, 코드는 "현재 상태"를 담당한다.** 둘이 어긋나면 코드가 정답이고
  SCHEMA.md를 고친다. 컬럼 목록을 손으로 이중 관리하지 말 것.
- **사용자가 만든 데이터와 빌드 산출물을 같은 저장소에 두지 않는다.** 산출물을 재생성하는 순간
  사용자 데이터가 사라진다.

## 구조
- **디렉토리 뼈대 (파일 생성 위치의 최종 기준 — 프로젝트 세팅 Phase에서 확정 후 여기 갱신)**:
  ```
  <소스 디렉토리>
  tests/               ← 테스트
  docs/                ← REQUIREMENTS.md, SCHEMA.md, playbooks/
  work_log/            ← plan.md, Phase_XXX.md
  scripts/             ← 빌드·유틸 스크립트
  ```
  **이 트리 밖에 새 최상위 디렉토리/파일을 만들지 말 것.** 필요하면 사용자 확인 후 이 목록부터 갱신.
- 설정/환경: `<로컬 vs 운영 분리 방식>`. 환경별 설정 파일은 값이 다르다 →
  **통째 복사 절대 금지**, 필요한 키만 한 줄씩.
- 새 기능 추가 위치 규칙: `<예: 도메인별 모듈에 추가, 거대 단일 파일 금지>`
- **같은 규칙이 두 곳 이상에 구현되면**(언어가 달라 이식이 불가피한 경우 등) 그 사실을 여기 적고,
  **두 구현을 대조하는 테스트를 반드시 만든다.** 조용히 어긋나는 것이 가장 비싼 유형이다.

## 불변 규칙
- **Silent failure 절대 금지**: 모든 예외 상황을 명시적으로 로깅한다. 빈 except/catch 금지.
  넘어가야 할 정당한 이유가 있으면 그 이유를 로그+주석으로 남긴다.
  (조용히 삼킨 예외는 "0건 처리"를 "정상 완료"로 둔갑시킨다.)
- **조용한 절단 금지**: 결과를 상한으로 자르면 **잘랐다는 사실과 전체 건수를 함께** 돌려준다.
  "일부만 보여주면서 전부인 척"이 가장 알아채기 어렵다.
- **손으로 만든 데이터에는 자동 검증을 붙인다**: 사람이 쓴 매핑·목록은 반드시 틀린다.
- **비밀정보(API 키, 접속정보)는 이 파일·커밋·채팅에 절대 쓰지 않는다.** `.env`에 두고 gitignore한다.
- `<프로젝트 진행하며 확정되는 규칙을 여기 추가>`

## 검증 원칙
- **"됐다"는 실제 결과로 판단한다.** 빌드 성공·프로세스 살아있음은 근거가 아니다.
  화면을 보거나, 실제 응답을 확인하거나, 데이터를 직접 질의한다.
- **기능 완료 시**: REQUIREMENTS.md의 수용 기준을 하나씩 짚어 대조한다.
- **Phase 완료 시**: 전체 테스트 통과가 다음 Phase의 선행 조건.
- **테스트가 실제로 무언가를 잡는지 확인한다.** 통과만 보고 넘기면 아무것도 검증하지 않는 테스트를
  통과로 착각한다. **일부러 틀리게 만들어 실패하는 것을 한 번 본다.**
- 자동화 불가한 항목(실기기, 실결제 등)은 plan.md의 "사용자 실테스트 대기"에 남기고,
  **확인 전 완료 선언 금지.**

## 검증 게이트 ★도구가 아니라 목적을 지킨다
게이트는 "이 도구를 써라"가 아니라 "이 검증을 통과해라"다. 도구를 못 쓴다고 검증을 건너뛰면 안 되고,
도구가 없다고 작업이 막혀서도 안 된다.

| 게이트 | 1순위 도구 | 못 쓸 때 대체 수단 |
|---|---|---|
| 요구사항 스펙화 | requirements-analyst | 메인 agent가 REQUIREMENTS.md에 직접 작성 + 모호점 질문 |
| 수용 기준 대조 | spec-checker | 메인 agent가 `[ ]` 항목을 하나씩 짚어 대조 |
| Phase 통합 테스트 | qa-tester (foreground) | 전체 테스트 실행 + 실제 화면·응답 확인 |
| 코드 점검 | `/code-review` | 사용자 호출이 필요하다. 못 하면 로그에 남기고 넘어간다 |
| 보안 점검 | `/security-review` | **`origin` 원격이 필요하다.** 없으면 직접 점검하고 문서로 남긴다 |

- **대체 수단으로 갔으면 작업 로그의 '검증 결과'에 그 사실과 이유를 적는다.** 조용히 건너뛰지 않는다.
- 서브에이전트·스킬이 이 환경에서 실제로 되는지 **초기화 때 확인**하고 계획에 반영한다.
- **메인 agent**: 코드 개발 + Phase 완료 시 작업 로그 작성(컨텍스트를 갖고 있으므로 직접 쓴다).
- **PLAN MODE**: `plan.md` 최초 작성 및 스코프 변경 시 갱신.
- **넓은 조사는 서브에이전트 병렬로**(가능한 환경이면): 다-파일 탐색은 `Explore`·`general-purpose`를
  병렬 호출하고 메인은 **결론만** 보관한다. 메인 컨텍스트를 검색 덤프로 채우지 말 것.

## 작업 로그 원칙 (`work_log/Phase_XXX.md`) ★이 템플릿에서 가장 값이 큰 부분
- Phase 완료 시 메인 agent가 직접 쓰고, `plan.md` 체크박스에 `[x]` 표시.
- **코드에서 알 수 없는 것만 쓴다** (코드는 What, 로그는 Why):
  - **결정의 이유**: "A와 B 중 A 선택 — 이유: …" + **버린 대안**
  - **외부 제약**: 발급 절차, 인코딩 규칙, 플랫폼 제약 등 코드에 안 드러나는 것
  - **실패한 접근과 원인**: "X 시도 → 실패 → 원인 Y → Z로 해결" ← **가장 중요하다**
  - **향후 운영상 주의점**: 나중에 운영매뉴얼의 재료가 되도록
- **적지 않는 것**: 코드 구현 상세(코드가 있다), git 히스토리(git log가 있다), 테스트 목록(테스트가 있다)

## 테스트 규칙
- 테스트 실행 명령: `<이 스택의 실제 명령>`
- **수용 기준 = 테스트 케이스**: REQUIREMENTS.md의 각 `[ ]` 항목이 테스트 하나에 대응해야 한다.
- 데이터 관련 기준은 **실제 데이터에 직접 질의해** 검증한다(모킹으로 대신하지 않는다).
- 외부 API에 의존하는 테스트는 **고정 응답으로 도는 것을 기본**으로 두고, 실호출은 분리한다.

## 배포 체크리스트
**"배포"가 무엇인지는 프로젝트마다 다르다.** 세팅 Phase에서 정의해 아래를 채운다.
서버 배포일 수도, 앱 설치일 수도, 패키지 배포일 수도 있다.

1. 전체 테스트 통과
2. `<빌드 명령>`
3. `<배포·설치 명령>`
4. **실제 동작 확인** — `<확인 방법: 예 curl 200 / 실기기 화면 / 설치 후 실행>`
5. commit (+ 원격이 있으면 push)

## 알려진 함정 (상세: docs/playbooks/)
- **요구사항 없이 구현 시작 = 스코프 크리프의 시작**: 수용 기준 먼저, 구현은 그 다음.
- **과잉 설계(YAGNI 위반)**: "나중에 필요할 것 같아서"는 근거가 아니다.
- **템플릿에서 물려받은 기본값은 이 프로젝트 것이 아니다**: 안 맞는 허용 목록·명령어는
  초기화 때 갈아엎는다. **틀린 기본값은 빈 칸보다 나쁘다.**
- **명령 앞에 `cd`를 붙이지 말 것**: 작업 디렉토리는 유지된다. `cd X && ...` 복합 명령이나
  환경변수 접두사(`FOO=bar cmd`)는 허용 규칙에 안 걸려 매번 확인을 묻는다.
  환경변수는 `.claude/settings.json`의 `env`에 둔다.
- **문서에 적힌 API·필드를 기억으로 쓰지 말 것**: 실제로 열어보고 이름과 건수를 확인한다.
- `<프로젝트 진행하며 발견한 함정을 여기 한 줄 + 플레이북 링크로 추가>`
